//
// Created by Ray on 6/1/2022.
//

#include <iostream>
using namespace std;
/*
int main()
{
    int t;
    int n;
    string name[102];
    int volume[102];
    int minValue;
    int minIndex;
    int maxIndex;
    int maxValue;

    cin>>t;
    for (int i = 1; i <=t ; ++i)
    {
        cin>>n;
        int a,b,c;
        minValue=1000005;
        maxValue=0;

        for (int j = 0; j < n; ++j)
        {
            cin>>name[j]>>a>>b>>c;
            volume[j]=a*b*c;
            if(maxValue<volume[j])
            {
                maxValue=volume[j];
                maxIndex=j;
            }
            if(minValue>volume[j])
            {
                minValue=volume[j];
                minIndex=j;
            }
        }

        if(minIndex==maxIndex)
            cout<<"Case "<<i<<": "<<"no thief"<<endl;
        else
            cout<<"Case "<<i<<": "<<name[maxIndex]<<" took chocolate from "<<name[minIndex]<<endl;
    }

    return 0;
}
*/