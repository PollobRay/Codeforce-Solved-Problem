//
// Created by pollo on 3/27/2021.
//

/*
#include <stdio.h>
int main()
{
    int i,n,a,b,rem,r;

    scanf("%d",&n);

    for(i=1;i<=n;i++)
    {
        scanf("%d %d",&a,&b);
        if(a==1||b==1)
            printf("Case %d: %d\n",i,a>b?a:b);
        else if(a==2||b==2)
        {
            r=((a*b)/8)*4;
            rem=(a*b)%8;
            r+=rem>4?4:rem;
            printf("Case %d: %d\n",i,r);
        }
        else
            printf("Case %d: %d\n",i,((a*b)+1)/2);
    }

    return 0;
}
*/
