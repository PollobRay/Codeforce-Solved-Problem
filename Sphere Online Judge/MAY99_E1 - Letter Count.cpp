//
// Created by Ray on 7/6/2022.
//

#include<iostream>
#include<map>
using namespace std;
/*
int main()
{
    map<char,int>values; // freequency, letter
    string std;
    int frequency;
    char ch;

    cin>>std;

    for (int i = 0; i <std.size() ; ++i)
    {
        values[std.at(i)]=values[std.at(i)]+1;
    }
    ch=255;
    frequency=0;
    for(auto it:values)
    {
       if(it.second>frequency||(it.second==frequency&&it.first<ch))
       {
           ch=it.first;
           frequency=it.second;
       }
    }

    cout<<ch<<endl;


    return 0;
}
*/