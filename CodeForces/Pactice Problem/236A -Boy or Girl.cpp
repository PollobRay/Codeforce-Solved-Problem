//
// Created by pollo on 4/10/2021.
//

#include <iostream>
using namespace std;
/*
int main()
{
    bool isMale=false;
    char name[102],distinctCharacter[27]={0};
    int number=0;
    cin>>name;

    for (int i = 0; name[i]!='\0' ; ++i)
    {
        if (isupper(name[i]))
            name[i]=(char)(name[i]+32);

        if (distinctCharacter[(int)name[i]-97]==0)
        {
            distinctCharacter[(int)name[i]-97]++;
            number++;
        }
    }
    if (number&1)
        isMale=true;

    if (isMale)
        cout<<"IGNORE HIM!"<<endl;
    else
        cout<<"CHAT WITH HER!"<<endl;
    return 0;
}*/