//
// Created by Ray on 12/6/2021.
//
//just check all alphabet of english is appears or not if appears print Yes otherwise not

/*
#include<iostream>
using namespace std;

int main()
{
    int n;
    int alpha[27]={0};
    char ch;
    int countAlpha=0;

    cin>>n;

    for (int i = 0; i < n; ++i)
    {
        cin>>ch;

        if(isupper(ch))
            ch= tolower(ch);
        if(alpha[ch-'a']==0)
        {
            alpha[ch-'a']++;
            countAlpha++;
        }
    }

    if (countAlpha==26)
        cout<<"YES"<<endl;
    else
        cout<<"NO"<<endl;

    return 0;
}*/
