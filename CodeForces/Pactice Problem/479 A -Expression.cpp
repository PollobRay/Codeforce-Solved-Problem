//
// Created by Ray on 9/4/2021.
//
/*#include <stdio.h>

int abs(int n)
{
    if(n<0)
        return n*(-1);
    return n;
}
int main()
{
    int a,b,c;
    int sum1,sum2,sum3,sum4,sum5,sum6,maxSum;

    scanf("%d %d %d",&a,&b,&c);

    sum1=a+b+c;
    sum2=a*(b+c);
    sum3=(a*b)+c;
    sum4=(a+b)*c;
    sum5=a+(b*c);
    sum6=a*b*c;

    maxSum=(sum1+sum2+abs(sum1-sum2))/2;
    maxSum=(maxSum+sum3+abs(maxSum-sum3))/2;
    maxSum=(maxSum+sum4+abs(maxSum-sum4))/2;
    maxSum=(maxSum+sum5+abs(maxSum-sum5))/2;
    maxSum=(maxSum+sum6+abs(maxSum-sum6))/2;

    printf("%d\n",maxSum);


    return 0;
}*/
