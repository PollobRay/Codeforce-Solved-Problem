//
// Created by Ray on 12/9/2021.
//
//if user name is exits print user name + number of occurence -1 otherwise OK
#include<iostream>
#include <map>
using namespace std;
/*
int main()
{
    map<string,int> user;
    int n;
    char name[33];

    cin>>n;
    for (int i = 0; i < n; ++i)
    {
        cin>>name;

        if(user.find(name)!=user.end())
        {
            cout<<name<<user[name]<<endl;
            user[name]+=1;

        }
        else
        {
            user.insert(pair<string,int>(name,1));
            cout<<"OK\n";
        }

    }
}
*/
