//
// Created by Ray on 6/27/2022.
//

#include <iostream>
#include <unordered_set>
using namespace std;
/*
int main()
{
    int t, n,m;

    cin>>t;

    for (int i = 1; i <=t ; ++i)
    {
        cin>>n>>m;
        long long d;
        unordered_set<long long> candies;

        for (int j = 1; j <=n ; ++j)
        {
            cin>>d;
            candies.insert(d);
        }
        //unordered_set<long long>::iterator it;
        for (int j = 1; j <=m ; ++j)
        {
            cin>>d;
            //it=candies.find(d);
            if(!candies.count(d))//it==candies.end())
            {
                cout<<"NO"<<endl;
                candies.insert(d);
            }
            else
            {
                cout<<"YES"<<endl;

            }

        }
    }
    return 0;
}
 */