//
// Created by Ray on 7/7/2022.
//

#include<iostream>
#include<vector>
using namespace std;
/*
int countNegatives(vector<vector<int>>& grid)
{
    int totalNumberofNN=0;
    for(int i=0;i<grid.size();i++)
    {
        int pos=-1;
        for (int j = 0; j <grid[i].size() ; ++j)
        {
            if(grid[i][j]<0)
            {
               pos=j;
               break;
            }
        }
        if(pos!=-1)
        {
            totalNumberofNN+=grid[i].size()-pos;
        }
    }
    return totalNumberofNN;
}
int main()
{
    vector<vector<int> >value={{3,2},{1,0}};

    cout<<countNegatives(value);


    return 0;
}
 */