//
// Created by Ray on 5/29/2022.
//

#include <math.h>
#include<stdio.h>
/*
int main()
{
    int n;
    double firstX,firstY,prevPointX,prevPointY,pointX,pointY,radius,length=0;

    scanf("%d %lf",&n,&radius);
    scanf("%lf %lf",&pointX,&pointY);
    firstX=prevPointX=pointX;
    firstY=prevPointY=pointY;
    for (int i = 1; i < n; ++i)
    {
        scanf("%lf %lf",&pointX,&pointY);
        length+= sqrt((pointX-prevPointX)*(pointX-prevPointX)+(pointY-prevPointY)*(pointY-prevPointY));
        prevPointX=pointX;
        prevPointY=pointY;
    }
    length+= sqrt((pointX-firstX)*(pointX-firstX)+(pointY-firstY)*(pointY-firstY));
    length+=2*3.1416*radius;

    printf("%.2lf\n",length);


    return 0;
}
*/