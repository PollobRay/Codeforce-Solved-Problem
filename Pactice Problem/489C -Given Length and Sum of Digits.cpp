//
// Created by Ray on 7/22/2021.
//

#include <iostream>
#include<cmath>
using namespace std;
/*
int sumOfDigits(int number)
{
    int sum=0;
    while (number!=0)
    {
        sum+=number%10;
        number=number/10;
    }

    return sum;
}

int main()
{
    int m,s;
    bool isFound=false;
    int max;

    cin>>m>>s;

    for (int i =1*pow(10,m-1); i < 1*pow(10,m); ++i)
    {
        if (s==sumOfDigits(i))
        {
            if (!isFound)
            {
                cout<<i<<" ";
            }
            isFound=true;
        }

    }

    if (!isFound)
        cout<<"-1 -1"<<endl;
    else
        cout<<max<<endl;

    return 0;
}
 */