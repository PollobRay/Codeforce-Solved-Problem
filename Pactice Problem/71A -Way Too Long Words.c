//
// Created by pollo on 4/7/2021.
//


#include<stdio.h>
/*
int wordlen(char w[])
{
    int l;
    for(l=0;w[l]!='\0';l++);

    return l;
}
int main()
{
    int i,n,len;
    char word[105];

    scanf("%d",&n);

    for(i=1;i<=n;i++)
    {
        scanf("%s",word);
        len=wordlen(word);
        if(len>10)
            printf("%c%d%c\n",word[0],len-2,word[len-1]);
        else
            printf("%s\n",word);
    }

    return 0;
}
*/